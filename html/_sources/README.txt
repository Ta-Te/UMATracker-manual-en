# UMATracker-manual

```sh
pip install sphinx sphinxcontrib.youtube recommonmark sphinx_rtd_theme
pip install sphinx-autobuild

cd UMATracker-manual
sphinx-autobuild ./ ./_build
```

Access `http://127.0.0.1:8000/` and fun!